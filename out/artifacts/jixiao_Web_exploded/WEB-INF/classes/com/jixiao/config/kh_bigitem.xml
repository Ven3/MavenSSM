<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC "//UNKNOWN/"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kh_bigitem">

	<!-- 查询所有考核项目(大类) -->
	<select id="getAll" resultType="kh_bigitem">
		select * from kh_bigitem
	</select>

</mapper> 